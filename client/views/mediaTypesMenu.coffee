#Template.mediaTypesMenu.events
  #'change .item-media': (evt, template) ->
    ##console.log 'change .item-media'
    #id = $(evt.currentTarget).data 'media-trigger'
    ##console.log 'change .item-media id ', id
    #media = $(evt.currentTarget).val()
    ##console.log 'change .item-media media ', media
    ##data = Template.currentData()
    ##console.log 'data'
    ##console.dir data
    ##product = NikkiApp.products.get id
    ##product = Template.instance().currentProduct().get()
    #product = template.currentProduct.get()
    ##console.log 'change .item-media product'
    #console.dir product
    #options = _.extend product, media: media
    #template.currentProduct.set product.id, options
    ##console.log 'NikkiApp.products.set id, opts'
    #console.dir options
    ##NikkiApp.products.set id, options
    ##console.log 'change .item-media product after set'
    ##console.dir product
    #return
